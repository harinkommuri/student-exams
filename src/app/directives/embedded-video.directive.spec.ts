import { EmbeddedDirective } from './embedded-video.directive';

describe('SafeUrlDirective', () => {
  it('should create an instance', () => {
    const directive = new EmbeddedDirective();
    expect(directive).toBeTruthy();
  });
});
