import { TopicScoreDirective } from './topic-score.directive';

describe('TopicScoreDirective', () => {
  it('should create an instance', () => {
    const directive = new TopicScoreDirective();
    expect(directive).toBeTruthy();
  });
});
